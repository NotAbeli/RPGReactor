# AGENTS.md — Agonia Engine (RPG Reactor fork)

Форк [RPG Reactor](https://github.com/Psychronic-Games/RPGReactor) v0.96.0 (MIT). Редактор + рантайм RPG-движка, совместимый с проектами RPG Maker MV/MZ. Стек: NW.js, PixiJS v8, three.js (3D-редактор, лениво), Effekseer. Node >= 22.

## Команды (из `app/`)

- `npm test` — node:test (120 тестов), сьюты в `app/test/`- `npm run dev` — запуск редактора в NW.js (единственная живая сборка: дистрибутивной нет)
- `npm run build:win|build:mac|build:linux` — дистрибутив
- `node build-scripts/generate-plugin-catalog.js` — регенерация `app/plugins/catalog.json` (обязательно после добавления/удаления плагинов; тест упадёт, если забыть)
- `node build-scripts/convert-plugin-commands.js <проект> [флаги]` — мигратор легаси-команд (356/357 → нативные 700+) и утилиты проекта: `--plugin <имя>` (скоуп), `--dry-run`, `--reseed-agonia` (пересобрать `data/AgoniaEngine.json` из тюнинга engineModules), `--harvest-all` (все остатки манифеста → engineModules/disabledPlugins), `--print-order` (порядок загрузки без запуска игры)
- `node build-scripts/restore-mv-runtime.js <проект>` — вернуть MV-corescript из `rpgmaker-runtime-backup.zip` и перепатчить мост v2 (идемпотентен)

## Структура

- `app/src/` — редактор (ProjectManager, ProjectController, MapEditor, MapEditor3D, TilemapManager, PluginManager, LightManager, DatabaseAgoniaEditor, ...)
- `app/src/event/commands/agonia/` — реестр нативных команд (`NativeCommands.js`) + schema-driven диалог (`NativeCommandDialog.js`)
- `app/runtime/` — reactor-рантайм (**спящий**: проект на нём не запускается, но зеркальные command7XX поддерживаются)
- `app/plugins/` — каталог плагинов движка (50 .js + `catalog.json`)
- `app/build-scripts/` — сборка (worker_threads), генераторы, миграторы
- `specs/` — спецификации фич (см. `engine-plugin-catalog.md`)
- `AGONIA ENGINE/` — упакованная NW.js-сборка (в git не входит; **устарела** — не содержит фиксов моста)
- `Право на Жизнь - Актуал/` — игровой проект MV, **отдельный git-репозиторий**

## Архитектура: проект на MV-рантайме + мост Agonia (v2)

Игра работает на **родном MV-corescript** (Pixi 4, оригинальный ФПС/визуалы). В конец `js/rpg_managers.js` вшит сниппет `ProjectManager.MV_CATALOG_LOADER_SNIPPET` (маркер `__rpgReactorCatalogV2`):

1. Каталог плагинов: `<проект>/js/plugins/<name>.js` (override) → каталог движка по `file:///`. Резолв: env `RPGREACTOR_PLUGINS_DIR` → `enginePluginsDir` из `project.rpgreactor`.
2. Wrap `PluginManager.setup`: мерж `engineModules` из `project.rpgreactor` в `$plugins` (порядок сохранён: якоря только на status:true + dep-sort pending; разделители/выключенные записи не якорят) + мерж `data/AgoniaEngine.json` поверх параметров модулей (строкификация MV). Манифест проекта **пуст** — все 49 плагинов живут в конфиге проекта под git.
3. Нативные команды `Game_Interpreter.command700..751` (50 шт.): свет (700–709), камера (710–714), сундуки (715–719), лут (720/724), враги (721–723/749), движение (725/726), позиционный звук (728: OcRam AEX-источник на якоре-событии — дистанция/радиус/фейд/тип/автопан; без якоря играет на всю карту, стоп делегирует `clear_aex`; флаги читаются через `flag(v,dflt)` — `Number(v||1)` ломает 0!), утилиты (730–733/747/748), крафт/подсказки/метки (734–736/750), титулы/презентация (737/740–746/751). Делегируют в живую MV-цепочку `pluginCommand`.
4. **MV-контракт вызова**: `executeCommand` кладёт параметры в `this._params` и зовёт хендлер **без аргументов** — каждый command7XX обёрнут адаптером (`rrAdapt`), резолвящим params из `this._params`. Тесты зовут команды так же (без аргументов) — иначе регрессия не ловится.
5. Input-харднинг: пад-кнопка должна прожить 2 опроса (дрейф-спайк → фантомное «ok» на титуле), синтетические (`!isTrusted`) события стопаются на capture, при фантомном OK — разовый warn в консоль.
6. Спрайты презентации: Text Pop (740, цвета `\c[n]` из windowskin) и Слайды (741–745, wait-mode `agoniaSlide`).

БД «Движок Agonia» (`data/AgoniaEngine.json`, редактор в `DatabaseAgoniaEditor`): секции `spriter` → SuperDuperSpriter (редактор — отдельная вкладка «Спрайтер» в `DatabaseSpriterEditor`, под «Героями»: скины/позы/NPC с условиями, грид-выбором, живым canvas-плеером; коллекции хранятся в MV-формате строк — `normalizeAgoniaValue` пропускает массивы JSON-объект-строк verbatim, `loadAgonia` дочернеет недостающие секции сидом), `battle`/`dash` → вкладка «Бой» (`DatabaseBattleEditor`: карточки Melee/Projectile/Tracer + Dash Database — имена видит натив 726), `enemies` → вкладка «Враги» (`DatabaseEnemiesEditor`: карточки EnemyDatabase с сенсорами и custom rules — вложенный customRules есть ОДИНАРНЫЙ JSON-массив объектов, не двойная MV-строка!), `audio` → Аудио-студия (`AudioStudio.js`, окно из тулбара: библиотека bgm/bgs/me/se с волной и транспортом, микшер 6 каналов + правила автозапуска карт `Map Rules`, редактор эмбиент-зон `AudioStudioZones` — правит реальные `<aex>`-события карт; `Map Rules`/`Recent Tracks` — JSON-строки; reseed переносит studio-данные из существующего файла, а не дефолтит), `stamina` → SuperDuperMovement, `lighting` → SDLight, `camera` → SuperDuperCamera, `inventory` → SuperDuperInventory, `screen` → SuperDuperScreen (система). Сид: engineModules → манифест → **retiredPlugins** → дефолты (`agoniaSeedValues`; retired-слепки — единственный источник тюнинга retired-модулей); normalizeDataSystem добивает MZ-блоки MV-проектам (advanced/titleCommandWindow + ранний resize — MV-splash-плагины не зовут Scene_Boot.start).

## Экранная система (S1, завершена) — single writer

**Канон: разрешение/фуллскрин/заголовок/CRT принадлежат ТОЛЬКО БД-секции `screen`.** `RR.applyScreenResolution` в мосте перезаписывает все легаси-копии (SRD Game/Screen Resolution, Core Ш/В, Screen W/H) значениями из БД при каждом запуске; при retire мост берёт `SceneManager._screen*` на себя. Инвариант заморожен тестом `app/test/agonia-screen-frozen.test.js` (116 тестов): плагины живы → все копии == БД, система выключена; плагины retired → системные поля/шимы/CRT несут те же значения.

Retired (в `retiredPlugins`, слепок параметров + канонический orderBefore): **SRD_GameUpgrade, SuperDuperCore, SuperDuperScreen** — заменены системными частями моста:
- Хаб-шим SRD (`installSrdHub`): SRD-неймспейс (parse/requirePlugin/...), `Imported["SumRndmDde Game Upgrade"]=1.35` (OptionsCreator/SuperTools завязаны), GameWindowManager-стаб, тюнинг-порты (PIXI GC/scale/wrap, ImageCache, JsonEx, Decrypter, ResourceHandler — читают параметры из retired-слепка), boot-fullscreen. Читает retired-слепок, т.е. править тюнинг — через Plugin Manager при `--restore-retired` или прямо в слепке.
- Шим `SuperDuper.Core` (API построчно; потребители — Camera/Inventory/Save/Notification/Craft/Settings).
- Системный CRT (`installCrtFilter`): построчный порт SuperDuperScreen — per-scene фильтр, `filterArea` каждый кадр (без него — рябь), интерполяция, полный канал SUPERDUPER (`_superDuperConfig` на Game_System; Splash/GameOver пишут туда напрямую и он работает).

Retire/restore: `node build-scripts/convert-plugin-commands.js <проект> --retire <a,b> [--reason ...]` / `--restore-retired <a,b>` (round-trip бит-в-бит, позиция по orderBefore-префиксам). A/B-гейт визуального паритета: пиксельный дифф скриншотов ≥99.5% (стенд: два NW-прогона с фикс-таймингом, raw-BGRA diff).

## Системные модули (S2+) — загрузчик

Для retired-плагинов, чей функционал движку нужен целиком (слишком крупный для шима-порта): `RR.loadSystemModules` исполняет **тот же файл каталога** до всех плагинов (async=false DOM-порядок; порядок модулей = порядку ключей в реестре), с параметрами `слепок retired ← БД-секция` (строкификация MV). Не в `$plugins`, нигде не числится плагином; `_scripts` защищает от двойной загрузки при `--restore-retired`. Пока плагин жив как модуль — загрузчик пропускает (переходный режим, параметры идут через `applyAgoniaConfig`). Объявлены: `SuperDuperSpriter ← spriter` (голова реестра — оригинальная позиция #6, до всех), `SDLight ← lighting`, `SuperDuperMovement ← stamina`, `SuperDuperMovement_Addon` (после Movement), `SuperDuperCamera ← camera` (после Addon), `SuperDuperInventory ← inventory` (после Camera), `SuperDuperBattle`, `SuperDuperEnemies` (аппенд в конец — сохраняет исходные порядки перекрывающихся пар: Camera<Battle по Game_Map.setup, SDLight<Enemies по meetsConditions; JSON-блобы атак/врагов живут только в снапшоте, БД-секций нет), `AgoniaAudioRules ← audio` (после Enemies; тонкий модуль — правила автозапуска аудио на картах из БД-секции `audio`, зоны идут мимо — они реальные `<aex>`-события, играет OcRam). Гейты: SDLight — пиксельный паритет 100.00%/100.00%; Movement — **поведенческий** (пиксельный дифф шумный на анимациях): векторные суб-тайл позиции, dash ×2, CollisionMesh, стамина тикает; Camera — поведенческий (follow/zoom ×2, прицел ПКМ свитч 18 → CE#12, нативы 710/711/712/714 через живую цепочку) + визуальная проверка юзером; Inventory — поведенческий (открытие I, хотбар поверх HUD, drag&drop, сундуки 715–719, RMB → var 17) + визуальная проверка юзером.

**Готча systemic-module миграции (уроки S5):** модуль грузится ДО плагинов → любые плагины, перезаписывающие глобальное состояние целиком (`Input.keyMapper = {...}` в SuperDuper.Keyboard:applyToSystem) или зависящие от порядка `addChild` на сцене (z-order: SRDuper HUDMaker `_hud` vs хотбар), инвертируют поведение. Лечение — идемпотентная перерегистрация в `Scene_Map.start` (после `ConfigManager.load`, работает при любом порядке алиасов). Регистрация в `Scene_Boot.start` мертва в этом проекте (splash не зовёт boot-цепочку).

**Редактор БД (S14–S23): навигация — 8 вкладок** — «Спрайтер» (первая подкатегория **«Герой»»: карточка актёра #1), «Бой» (S27: классический list-стиль как Инвентарь — 5-кнопочный бар Ближний бой/Снаряды/Трассеры/Рывки/**ИИ Врагов** над панелями, все секции через общий драйвер `_renderClassicCollectionTab` (`classicApi(kind)`×4 у BattleEditor, `classicApi()`+`globals` у EnemiesEditor — глобальные настройки = постоянная строка «⚙ Глобальные настройки» в списке), без строки поиска — короткие списки; `openDatabase('enemyAI')` редиректит в Бой), **«Инвентарь»** (S23: Предметы+Инвентарь объединены — полноширинный 6-кнопочный бар Предметы/Оружие/Броня/Крафт/Лут/Подарки над всем контентом, `showInventoryUnifiedTab`; с S26 **все 6 режимов в классическом viewer-стиле** (с S27 заголовки секций сокращены: «Лут», не «Инвентарь — Лут»): Предметы/Оружие/Броня — `showDatabaseViewer`, Крафт/Лут/Подарки — общий драйвер `_renderClassicCollectionTab` через те же реальные контейнеры `#database-list-panel`/`#database-detail` с точным хромом viewer'а (поиск, «Новый»/«Удалить», плоские строки `database-list-item` с #N, confirm при удалении); редакторы отдают `classicApi()` {entries/persist/blank/label/search/renderDetail} вместо монтирования MasterDetailShell; `commit()` зовётся и из вложенных таблиц — правки сразу персистят внешнюю MV-строку коллекции; проба S25 (первые три на shell) отклонена и откачена `7d5412b`; `openDatabase('items'|'invSystems')` редиректят в `inventory`; в карточке предмета — секция **«Теги»** sptags/spdisposable с синхронизацией note и suggestions), **«Мир»** (Шаги/Переменные/Дроп + **Надписи на экране** как 4-й под-таб: хинты/титулы/попапы/уведомления), **«Интерфейс»** (UI-студия S16: инвентарь-Visual/Sound, Сохранение/Титул/Заставка/Гейм-овер/Сообщения/Выборы/Настройки/Крафт-UI — data-driven `DatabaseUIEditor`, FIELD_DEFS из @param-блоков; HUD — кнопка к HUDMaker F9), «Тайлсеты», «Общие события», **«Система»** (S22: System 1 + Agonia Engine объединены — под-табы «Общие» (System 1 + Asset Sizes, `showSystem1Detail(container,{noBanner})`) / «Движок» (Agonia, `showAgoniaDetail(container,{noBanner})`); `openDatabase('system1'|'agonia')` редиректит в `system`). **S17 унификация UI:** общий словарь `AgoniaLabels` (DatabaseDataEditors.js) переводит английские имена параметров («Rec SE Vol» → «Запись · звук · громкость») — все FIELD_DEFS-поля и группы через него; `_stdBanner` — единый баннер вкладок; числовые поля фиксированной ширины. Все S15/S16-плагины в переходном режиме (БД ведёт параметры живого плагина через `applyAgoniaConfig`); нативы 720/724/751 имеют suggestions из БД; на событии — чекбокс «Сохранять позицию» (YEP). Terms/Types/System2 — вкладки убраны, `DatabaseSystem2Editor.js` удалён. Вкладки Skills/Classes/Enemies(MV)/Troops/States/Animations убраны (S12/S13). **S19 «Инспектор+Таблицы»** (гибрид Unity-inspector/Airtable, слайдеры): общие компоненты в `DatabaseShells.js` — `InspectorForm` (строки подпись-слева/контрол-справа, секции-подписи с линией, ноль вложенных панелей), `SliderNumber` (range+число синхронно; типуемое значение может выходить за диапазон, clamp только у ползунка), `DataTable` (inline-правка ячеек с коммитом ДО rerender — иначе blur съедает значение; зебра, sticky-заголовок, сортировка кликом, ▶-раскрытия, ▲▼✕; при активной сортировке ▲▼ скрыты). Применение: **Бой** — master-detail (список слева) + инспектор со слайдерами (радиус/ширина/длительность/скорость/хитбокс/кулдаун/заряды); **ИИ врагов** — глобальный инспектор + master-detail, враг = секции База/Сенсоры/Паника (слайдеры), customRules — вложенная DataTable с ▶-инспектором правила; **Мир** — террейны шагов/пулы звуков/реактор-группы/реакции/decay/autooff и Надписи (хинты/титулы/уведомления) — DataTable, плоский тюнинг (настройки шагов, дроп, попапы) — инспекторы; **Предметы** — форма на инспектор-строках, data-field/data-ce/data-sptags контракты слушателей не тронуты. Дата-слой (кодеки MV, seed, sptags note-sync) не менялся. Смоук: `%TEMP%\opencode\s19-smoke.js` (12 рендеров, setTimeout исполняет колбеки — под-табы рендерятся по-настоящему). **S20 «Дом-стиль + хвосты»** (`<S20a>`+`<S20b>`): `.agn-*` рескин под язык редактора — инспекторы в панелях `bg-panel`+рамка (agonia-section-вид), подписи секций 13px bold strong (без капса), таблицы в идиоме traits-table (видимые линии `border-input`, ячейки `bg-surface`, th 12px/600); ИИ врагов: глобальные настройки — правая sticky-колонка 360px рядом с master-detail врагов (узкое окно → уходит вниз); Инвентарь (Крафт/Лут/Подарки) и коллекции Спрайтера переведены на master-detail + DataTable (карточные деки S17 удалены); MasterDetailShell получил опцию `thumb` (миниатюра спрайта в списке — Спрайтер); Герой/Глобальные Спрайтера — инспекторы; грид поз и canvas-плеер не тронуты. **S21-фиксы:** Спрайтер чинился (S20-патч вырезал `_entryHeadline` → коллекции с записями падали = «пустая вкладка»; восстановлен, репро с данными) + красные баннеры ошибок вместо тихой пустоты (`render()` Спрайтера, `renderForm` MasterDetailShell); ИИ врагов переехал в Бой под-табом; DataTable при 0 записей — компактная строка «пусто · +Добавить» вместо каркаса таблицы. **S22-фиксы:** Инвентарь-краш (ингредиенты крафта — СТРОКИ ID, S20 ошибочно скормил их как объекты — теперь таблица имён с select в раскрытии; select-колонкам лута/подарков переданы `options: itemOptions` — `col.options.map` падал; guard `(col.options||[])` в DataTable); Спрайтер откачен на карточный вид `0173ab5` (живое превью-анимация в каждой карточке) + кап ширины карточек 1100px + баннер ошибок; System 1 + Agonia → «Система» (9 вкладок).

Reactor-рантайм (Pixi 8) — **не выкатывать на проект**: опробован, ломает ФПС/визуалы (CRT-шейдер, legacy super-вызовы). Правки зеркалятся туда для будущего, но рабочий путь — MV-мост.

## No-Battle (S12+S13) — стандартный бой ампутирован

Проект — экшен-RPG (бой через SuperDuperBattle), Scene_Battle недостижим и вырезан: **рантайм** — модуль `RR.installNoBattle` в мосте: 301 + enemy-ops (331–337/339/340/342) + боевые системные команды (132/133/136/139 — боевые BGM/ME и энкаунтеры, 212/283 — анимации и боевой фон, 313/326 — состояния и TP) → no-op, `updateEncounter`/`encounterProgressValue` отключены, `isBattleTest`=false, `BattleManager.setup/startBattle` — warn-заглушки; боевые UI-классы (`Scene_Battle`, `Spriteset_Battle`, `Window_Battle*`, `Window_Party/ActorCommand`) затираются на **первом `Scene_Map.start`** (не в setup! — async=false скрипты плагинов исполняются после возврата setup, а HUDMaker/Camera алиасят Scene_Battle при загрузке). `Game_*` (Battler/Actor/Troop) НЕ тронуты — JsonEx сейвов требует живых конструкторов. **Редактор**: вкладки Skills/Classes/Enemies(MV)/Troops/States/**Animations** убраны из навигации; 22 боевых/анимационных редактора удалены файлами (диспетчеры вычищены); из палитры — 301/313/318/321/326/331–337/339/340/342; из ConditionalBranch — Class/Skill-условия; поля анимаций атаки убраны из Item/Weapon/Skill; System1 без Battle Screen/Battle System секций и боевых строк музыки/звуков (данные System.json не тронуты). **Данные проекта**: `Animations.json`/`States.json`/`Enemies.json`/`Troops.json` — стабы `[null]` (DataManager грузит файлы — пустить нельзя), `img/animations/` удалена (снаряды SuperDuperBattle живут в img/pictures). **Живое и не трогать**: баллуны (213, используются в CE/картах), Weapon/Armor вкладки (инвентарь хранит эти типы), эффекты предметов (useItem лечит), HP/MP/EXP/Level (311–317). **Готча**: типы вкладок БД обязаны быть уникальны — S10-коллизия `enemies` делала стандартный редактор врагов недостижимым (теперь у ИИ-врагов тип `enemyAI`).

## Готчи (проверено на этой машине)

- **«Два значения 48»**: 48 — размер тайла в px (менять можно) и формат tile ID автотайлов (`(tileId - 2048)/48`, `% 48` — менять НЕЛЬЗЯ).
- `fs.rmSync` оставляет файл в листинге (delete-pending) — использовать `fs.unlinkSync` с rmSync-fallback.
- Кириллица в `node -e` из PowerShell ломается — пути передавать через UTF-8 файл-скрипт; .ps1 с кириллицей — UTF-8 **с BOM**.
- `app/src` и `app/runtime` — браузерные глобальные скрипты; тестировать через `node:vm` с моками `document`/`process`/`require` (см. `app/test/mv-native-bridge.test.js`).
- Мост-сниппет правится в `ProjectManager.MV_CATALOG_LOADER_SNIPPET`, **не** в патченном `js/rpg_managers.js` проекта; деплой — `restore-mv-runtime.js` (перепатчивает идемпотентно).
- **Сниппет-генерация шейдеров/длинных строк**: собирать массивом строк с `.join("\n")` (как в плагине), не однострочным литералом — двойное экранирование `\n` даёт шейдер, который не компилируется на GPU (ловится только живым прогоном; в vm-моках Pixi-стаб молчит).
- **Пробы в NW**: весь код проб в try/catch — неперехваченная ошибка из setInterval ловит глобальный error-handler MV → `SceneManager.stop()` → «игра не запускается», выглядет как баг моста. Глобал моста — `window.__RRMVBridge` (внутренний `RR` не экспортирован).
- Не параллелить NW-плейтесты с правками файлов проекта: старые окна держат прежний код в памяти и врут в консоли (стек-номера строк от прежней версии файла).
- Дистрибутивная сборка редактора устарела/удалена; **БД «Движок Agonia» открывать только из `npm run dev`**, пока сборка не пересоздана (старый сид перезаписывал AgoniaEngine.json дефолтами).

## Git

- Теги: `fork-baseline` (f281fbb), `pre-plugin-migration` (4e5e2ab), `pre-native-commands` (= fork-база перед командами), `native-commands-baseline` (bbff1cd: мост v2 + input-харднинг, 93 теста).
- S1-экранная система завершена 2026-08-15: движок `d5367ac`+ (116 тестов, заморозка single-writer), проект `c2859fa` (3 плагина в retiredPlugins). S2-свет завершена 2026-08-15: движок `65348a7` (120 тестов, системные модули), проект `1206694` (SDLight в retired → системный модуль). S3-движение завершена 2026-08-15: движок `2c56483`, проект `f39e8ce` (Movement+Addon в retired → системные модули). S4-камера завершена 2026-08-15: движок `8d13e8b` (121 тест, камера в реестре системных модулей), проект `e889b0f` (SuperDuperCamera в retired → системный модуль; rpg_managers repatch обязателен — restore-mv-runtime). S5-инвентарь завершена 2026-08-15: движок `f5663f4` (122 теста, инвентарь в реестре + keyMapper/z-order харденинг), проект `724c4d7` (SuperDuperInventory в retired → системный модуль). S6-бой завершена 2026-08-15: движок `d6e43bf` (123 теста, Battle+Enemies в реестре), проект `f481298` (SuperDuperBattle/SuperDuperEnemies в retired → системные модули; гейт: melee-хитбокс, SDB.DB блобы, нативы 721/723/749 + визуальная проверка юзером). S7-редактор спрайтера завершена 2026-08-15: движок `3f848d9` (128 тестов, вкладка «Спрайтер» в БД + секция spriter), проект `fdc6b1b` (сид секции из живого тюнинга + repatch). S8-спрайтер-модуль завершена 2026-08-15: движок `f995475` (129 тестов, Spriter головой реестра), проект `28178c1` (SuperDuperSpriter в retired → системный модуль; A/B-паритет module≡plugin подтверждён, скины требуют свитч-условий — не только var 17). A9-аудио-студия (Stage A) завершена 2026-08-15: движок `dc4277e` (133 теста, AudioStudio + AudioStudioZones + AgoniaAudioRules в реестре + секция audio с reseed-preserve), проект `71ba084` (repatch). S10-боевые секции завершена 2026-08-15: движок `ccc6f3f` (151 тест, вкладки «Бой»/«Враги», секции battle/enemies/dash, сид из retiredPlugins, мост ведёт Battle/Enemies/Addon), проект `daa042c` (сид из снапшотов: 2 melee/1 proj/1 tracer/3 enemies/4 dashes). S12-ампутация боя завершена 2026-08-16: движок `5decbb6` (154 теста, No-Battle модуль + чистка редактора), проект `9376f92` (Enemies.json стаб + repatch). S13-анимации и хвосты завершена 2026-08-16: движок `6bd4fbb` (−12k строк: 22 файла боевых/анимационных редакторов, палитра/System1 чистка, мост no-op + зеркало рантайма), проект `b7e9f25` (Animations/States стабы, img/animations −52 МБ). S14-компактная БД завершена 2026-08-16: движок `74dadef` (навигация 8 вкладок, Герой в Спрайтере, Инвентарь 3-в-1, System2Editor удалён, Asset Sizes → System 1). S15-дожатие данных завершена 2026-08-16: движок `4be5ada`+`d62c9ab` (158 тестов, 9 новых секций: craft/hints/popup/loot/gifts/steps/variables/drop/notification, 11 вкладок, YEP-галочка, suggestions 720/724/751), проект `51bd66c`+`4bd9597`. S16-теги+UI-студия завершена 2026-08-16: движок `71d9e69` (161 тест, секция sptags в предметах, «Интерфейс»: 8 секций save/title/splash/gameover/message/choices/settings + craft-UI, FIELD_DEFS из @param-блоков), проект `72aeb13` (сид UI-тюнинга из живых параметров). S17-реорг+кит: `06a06bd`+`e21af7b`+`5ec4f36`+`d43bff0`+`f2d5988` (AgoniaLabels, agonia-* CSS-кит). S18-переписывание: `95a22cc` (DatabaseShells v1: MasterDetail+Accordion). S19-инспектор+таблицы завершена 2026-08-19: движок `c201cfb` (InspectorForm/SliderNumber/DataTable, слайдеры, смоук 10/10, дата-слой не тронут — 161 тест). S20-дом-стиль+хвосты завершена 2026-08-19: движок `39c6065`+ (рескин agn-кита, враги-глобалы правой колонкой, Инвентарь/Спрайтер на master-detail+DataTable, thumb-миниатюры). S21-фиксы завершена 2026-08-22: движок `67fdf2a` (Спрайтер `_entryHeadline` восстановлен + баннеры ошибок, ИИ врагов → под-таб Боя, DataTable пустые = строка). S22-фиксы завершена 2026-08-22: движок `+` (Инвентарь-краш, Спрайтер откат на карточки+кап, Система-вкладка). S23-унификация завершена 2026-08-22: движок `+` (ИИ Врагов pinned-глобалы пиксельно единый с под-табами Боя, Предметы+Инвентарь → одна вкладка с 6-кнопочным баром). S25-инвентарь-shell откачена 2026-08-22: `b1ae8a6` → revert `7d5412b` (перевод Предметов/Оружия/Брони на shell отвергнут юзером — классический viewer вернулся). S26-инвентарь-классика завершена 2026-08-22: движок `9a2b187` (Крафт/Лут/Подарки переведены в классический viewer-стиль через classicApi + реальные list-panel контейнеры). S27-бой-классика+заголовки завершена 2026-08-22: движок `face42a` (заголовки секций сокращены; Бой и все его секции переведены на классический list-стиль: общий драйвер _renderClassicCollectionTab с noSearch/globals, classicApi у Battle/EnemiesEditors, 5-кнопочный бар; MasterDetailShell больше не используется вкладками). S28-центр-колонка завершена 2026-08-22: движок `99ba8e3` (детali-формы — центрированная колонка 1100px `.agn-detail-col` во всех обёртках, секции инспектора ≥4 строк текут в 2 колонки `.agn-insp-cols` (auto-fit 520px, формы вдвое короче), превью графики предметов кап 320px). S29-спрайтер-классика завершена 2026-08-22: движок `39bf861` (Спрайтер на общем паттерне: 5-кнопочный бар Герой/Скины/Позы/NPC/Глобальные, коллекции через `_renderClassicCollectionTab` с новой опцией `rowIcon` — миниатюры спрайтов 28×36 в строках, формы в agn-detail-col (Запись→Условия→Визуал), карточный дек ~−250 строк удалён, Глобальные — инспектор, живой плеер/грид поз не тронуты). S30-спрайтер-грид завершена 2026-08-22: движок `85acbf4` (классический список заменён ГРИДОМ ЖИВЫХ КАРТОЧЕК: `renderCollectionGrid` — auto-fill 160px мини-плееры + имя + сводка условий, форма справа 460px с ▲▼⧉✕; клик карточки не останавливает грид-плееры — рециклятся только тегированные 'spriterForm'; `_renderPlayer(opts.mini)`; Герой вырезан — базовый облик = скин MainValue=−1, бар 4 кнопки Скины/Позы/NPC/Глобальные; классический список остаётся только в Бое/Инвентаре) S31-позы-статичны+визуал-сверху завершена 2026-08-22: движок `aadc1da` (плеер позы — ОДНА статичная ячейка GridX/GridY: без покадрового тика и контролов воспроизведения; порядок формы head→Визуал→Условия→Запись — Визуал открывается превью (живой плеер скинов/NPC, статичная ячейка поз) + файл/индекс + свёрнутый «▸ Продвинутая анимация» `<details>` (8 тайловых полей + режимы + ручные индексы, 0=авто); имя/приоритет — метаданные внизу; `InspectorForm.setTitle` — живое обновление заголовка при правке имени). S32-гибкие-условия+форма завершена 2026-08-22: движок `2aa6475` (**плагин SuperDuperSpriter расширен**: `Conditions.Checks` — единый список проверок [{type:'switch',id}|{type:'var',id,op,val}] любой длины, все И; legacy SwitchId1..3/ExtVarId конвертируются при чтении; watchedVars собирает все var-проверки; тест-хук `__SDS_TEST`; тест `agonia-spriter-conditions` 5 кейсов — итого 166); редактор: конструктор условий (строки тип/id/op/val/✕ + «+ Условие», миграция legacy при первом чтении, живая сводка в head из Checks); форма скина: head = «#N · условия» (имя без дубля), превью → Название+Приоритет под графикой → файл (скины: файл И ячейку персонажа в одном двухшаговом пикере `pickCharacterIndex`, поле индекса удалено) → свёрнутая «Продвинутая анимация» → Условия; секция-заголовок «Визуал скина» удалена). Плейтест скинов живьём при следующем запуске (изменение аддитивное). Теги: `s1-screen-complete`, `s2-light-complete`, `s2-light-final`, `s4-camera-complete`, `s5-inventory-complete`, `s6-battle-enemies-complete`, `s7-spriter-editor-complete`, `s8-spriter-module-complete`, `a9-audio-studio-stage-a`, `s10-battle-db-complete`, `s12-no-battle`, `s13-animations-out`, `s14-db-compact`, `s15-data-plugins-a`, `s15-data-plugins-complete`, `s16-ui-studio`, `s19-inspector-tables`, `s20-house-style`, `s21-fixes`, `s23-unify-tabs`. S33-форма-финал завершена 2026-08-22: движок `8ed0045` (строка-номер над превью удалена полностью — над графикой ничего; порядок превью → файл → Название+Приоритет → свёрнутая анимация → свёрнутые Условия; превью всех видов — одна рамочная панель 360px фикс с равной высотой сцены; Условия — `<details>` с живой сводкой в заголовке «▸ Условия · var=1 · sw 5», конструктор проверок внутри блока). S34-порядок+ширина+шум завершена 2026-08-22: движок `571169b` (Название/Приоритет — ВЫШЕ «▸ Продвинутой анимации» (блок вынесен из _renderSpriteVisuals в _renderAdvancedAnimation); превью — width:100% колонки, той же ширины что остальные блоки (было 360px фикс); подписи полей файла вырезаны («Файл и персонаж спрайта»/«Файл спрайта») — голая строка input+placeholder под превью, кнопка пикера «…» с title-подсказкой). S35-память-выбора завершена 2026-08-22: движок `a054bc8` (запись ВСЕГДА открыта при входе в секцию: `_classicSelMemory` по memoryKey в драйвере — восстанавливается последняя открытая запись (включая строку «⚙ Глобальные»), иначе первая; детali рендерится сразу при монтировании, без заглушки; Новый/Удалить обновляют память (удаление → сосед); фильтр поиском память не трогает; спрайтер-грид — та же память по kind (`_gridSelMemory`): клик/▲▼/⧉/✕/Новый запоминаются). S36-пкм-меню завершена 2026-08-22: движок `8539ca1` (шапка формы Спрайтера (номер+имя+▲▼⧉✕) вырезана — личность записи и так на карточке; инструменты переехали в ПКМ по карточке: «▲ Выше/▼ Ниже» (disabled на краях)/«⧉ Дублировать»/«✕ Удалить» (confirm) — тот же пайплайн persist+memory+rebuild; форма начинается сразу с превью; onMeta живьём обновляет подпись ВЫБРАННОЙ КАРТОЧКИ (имя+сводка), а не удалённой шапки). S37-анимация-типы завершена 2026-08-22: движок `7b8aa18` (поля «▸ Продвинутой анимации» по доменам: Кадров — селект 3/4/5/6 (нештатное текущее значение остаётся видимым), Направления — селект «Крутится (4)/Фиксировано (1)» (единственные валидные для плагина), Idle индекс — селект «Выкл/0..7»; FPS/Idle-скорость/Задержка — численными; Ширина+Высота — парная строка 2 колонки; сетка [Кадров|Направления|FPS]/[Ш|В]/[Idle|скорость|задержка]; дата-слой не тронут — селекты пишут те же числа). S37b-фикс (сразу за): `540af5c` — ряды S37 размазывали форму: голые div-гриды без скоупленного `min-width:0` у ячеек, длинные опции селектов распирали 1fr-треки; ряды переведены на класс `agonia-field-grid` (мин-ширина ячеек из кита, инлайн-колонки поверх auto-fill); ВСЕ мелкие подписи-подсказки в блоке вырезаны (остались только label-имена полей). S38-вырезка-оружия/брони+теги-чипы завершена 2026-08-22: движок `68f62fd`, проект `2c55df1` (Оружие/Броня вырезаны из игры: бар Инвентаря 4 кнопки Предметы/Крафт/Лут/Подарки, редакторы DatabaseWeaponEditor/DatabaseArmorEditor/DatabaseItemBaseEditor удалены файлами, ветки/скрипты диспетчера вычищены; в ПРОЕКТЕ Weapons.json/Armors.json → стабы `[null]` — экипировка актёров нулевая, команд 132/133 нигде нет, инвентарь/лут/подарки/крафт ссылаются только на предметы; теги предметов — ЧИПЫ-ПЕРЕКЛЮЧАТЕЛИ из готового списка (клик = добавить/снять, `<sptags:…>` синхронизация как раньше) + «+» создать новый тег инлайн-инпутом (Enter/✓, Esc/✕, в нижний регистр), текстовый ввод с datalist удалён, spdisposable-чекбокс остался). S39-HUD-редактор завершена 2026-08-22: движок `2ab3ae5` (секция HUD в «Интерфейсе» — живой редактор `DatabaseHUDEditor` без плейтеста: шахматка-transparency grid в разрешении игры, рендер трёх типов кусков MapHUD.json (Picture/Image Numbers-полоска 10 цифр/Image Gauge с Direction), условия/значения — JS в песочнице с моками, панель «Состояние игры» (авто-сбор sw/var ID из выражений — тумблеры/поля), список кусков + клик-выбор + перетаскивание = x/y + инспектор, персист MapHUD.json с таймстамп-бэкапом — только поля HUDMaker (он остаётся fallback); новая вкладка «Витрина» — мок-хотбар инвентаря на той же шахматке из параметров, плагины не тронуты). S40-фикс завершена 2026-08-22: движок `43d9a2f` (почему «просто шахматка»: S39 выдумал пути `hud/Picture` — реальных папок нет, все куски 404; условия скрывали почти всё; gauge читал несуществующее поле Direction). Фиксы: картинки из настоящих папок HUDMaker `img/SumRndmDde/hud/{pictures,numbers,gauge_images,gauge_backs}`; мастер-тумблер «Показывать все» вкл по умолчанию (выкл — условия + состояние игры); gauge заполняется по полю `Style` (left/right/up/down); мок-хотбар инвентаря рисуется на ТОЙ ЖЕ шахматке канвасом-подложкой под кусками (клики сквозь) — вкладка «Витрина» удалена; HUD — первая и активная вкладка «Интерфейса»). S41-масштаб+окна завершена 2026-08-22: движок `63b72ca` (селект масштаба «Вписать»(авто)/100/75/50 — transform+scaler-обёртка, клики точны при любом k; РЕДАКТИРУЕМЫЙ СЛОЙ ОКОН на общей шахматке под кусками HUD: 8 якорей — крафт (слоты блоком по Slot 1 X/Y+Spacing, результат, хинт с текстом, превью; картинки из img/pictures) и инвентарь (Visual Settings-блоб СЕЕТСЯ из дефолтов плагина при первом касании, MV-строка: окно игрока Cols×Rows, сундук, хотбар — drag только Y (плагин центрует X), кастомное окно); клик-выбор окна + drag пишет ключи секций через обычный сайдкар-пайплайн; инспектор окна (позиции + шаги сетки); фикшен бесконечный цикл рендера — запись кэша картинок ДО присвоения src). S42-точная-геометрия завершена 2026-08-22: движок `82e81e4` (окна инвентаря/крафта — по РЕАЛЬНЫМ позициям игры: Visual Settings-блоб СЕЕТСЯ из retired-слепка project.rpgreactor (реальный тюнинг: Player 974/195 на inventoryBackground 250×335 c Slot Offset 25/25, сундук 5×7 @ 50/195, кастомное окно opisaniye @ 998/380, Hotbar Y 690) — дефолты аннотации только как фоллбек; окна игрок/сундук рисуют реальный фон натурального размера + сетка слотов X+OffX+col×Spacing (InventorySlot_00 = 38px); хотбар — как в плагине: anchor 0.5 (Y = ЦЕНТР, X авто-центр), ровно 5 слотов × hbSpace×hbScale, drag пишет центр; крафт — pct-фоллбеки плагина (pctX(23)/pctY(14)/spacing pctX(5)/result pctX(43)), Hint/Preview — полноширинные 40px текст-строки; инспектор окна игрока + поля «Отступ слотов X/Y») S43-сепарация-инвентаря завершена 2026-08-22: движок `2c978e7` (**плагин SuperDuperInventory расширен**: `Visual Settings` + ключи `Grid2 X/Y/Bg` — ряд 1 (хотбар-ряд) на Player X/Y, ряды 2–4 независимым блоком на Grid2 (дефолты = прежняя раскладка пиксель-в-пиксель, фон Grid2 Bg своей пластиной; createSlots ветвится по row — индексы слотов/хит-тесты/драг не тронуты); редактор: `inv.player` = ряд 1 (1×cols), новый перетаскиваемый якорь `inv.grid2` (3×cols, свой фон, инспектор X/Y/фон/cols/rows/шаг), Grid2-ключи сеются в блоб при первом касании (пустые placeholder-строки = не задано, коммит сразу); `inv.custom` дорисовывает серый регион вывода инфы (имя Name X/Y + описание Desc X/Y, ширина Desc Width) ВНУТРИ окна — прямоугольник едет только вместе с окном информации). Плейтест раскладки при следующем запуске. S44-складной-фон+снап завершена 2026-08-22: движок `25f4a9c` (дубль фона починен СКЛАДНОЙ НАРЕЗКОЙ: S43-дефолт Grid2 Bg=Player Bg рисовал всю пластину 250×335 дважды — теперь ряд 1 рендерит верхний срез 1/rows (плагин: addLoadListener+setFrame; редактор: source-crop drawImage), ряды 2–4 — нижний срез ТОЙ ЖЕ картинки; Grid2 Bg пуст по умолчанию (задал = своя картинка целиком); редактор получил прилипание как в HUDMaker: порог 12px, кандидаты — центр сцены + лево/центр/право и верх/центр/низ всех прочих кусков и окон, красные полноэкранные направляющие при активном снапе, гаснут на mouseup; работает для кусков HUD и окон). S45-фон-прибит-к-ряду1 завершена 2026-08-22: движок `ea88b2b` (по юзеру: ВЕСЬ задник инвентаря ГГ прибит к первым пяти ячейкам — нарезка S44 вырезана; плагин рендерит фон целиком на Player X/Y у блока ряда 1; ряды 2–4 по умолчанию ГОЛЫЕ слоты (rect от сетки 5×3), своя картинка Grid2 Bg — целиком на Grid2 X/Y; сток-раскладка не меняется: двигаешь ряд 1 — едет весь фон, двигаешь ряды 2–4 — едут только слоты). S46-сохранение+стрелки+шум завершена 2026-08-22: движок `d967169` (КОРЕНЬ «правки не переносятся в игру»: `normalizeAgonia` собирал каждую секцию ТОЛЬКО из ключей agoniaDefaults — добавленные редактором ключи (Visual Settings-блоб, Grid2 X/Y/Bg…) молча выкидывались при каждом OK/Apply; normalize теперь пропускает неизвестные ключи известных секций verbatim; стрелки двигают выбранный кусок/окно на 1px (Shift = 10px) с персистом после нажатия, поля ввода исключены; подпись «имя / описание предмета» на сером прямоугольнике вырезана). S47-фон-отдельно+слои+появление завершена 2026-08-22: движок `fd867ef` (фон инвентаря и сундука — ОТДЕЛЬНЫЕ перетаскиваемые якоря: новые ключи `Player Bg X/Y` / `Chest Bg X/Y` (дефолты = сток), `inv.player`/`inv.chest` — голые слоты, якоря `inv.bg`/`inv.chestBg` — пластины; z-приоритеты `Z Player/Z Chest/Z Hotbar` — плагин пересортировывает setChildIndex после создания и спавна хотбара, диммер всегда ниже, 0 = сток-порядок + чекбокс `Inventory Over HUD` поднимает блок над _hud HUDMaker'а; появление: селект «Мгновенно 255/Быстро 60/Средне 35/Медленно 15» + число → Fade Speed, Dimmer Opacity, Hover Opacity; фикс переполнения X/Y — компактный кит `.agn-insp--tight` (112px подпись, 64px число) для инспекторов HUD-панели). S48-юзер-френдли завершена 2026-08-22: движок `3853709` (единый список слева: группы «Куски HUD»/«Окна интерфейса» — все 11 окон кликабельны с человеческими именами (Фон инвентаря/Ряд 1 (хотбар)/Ряды 2–4/Фон сундука/Слоты сундука/Хотбар/Окно информации/Крафт — ×4) и тегами типов; 👁 глазик на каждой строке — скрытие ТОЛЬКО в редакторе (_hiddenPieces/_hiddenWindows: пропуск рендера и пика, данные не тронуты); постоянная строка-подсказка под сценой; плавающий бейдж имени у выбранного объекта + рамка выделения теперь и у окон; инспекторы перегруппированы в секции Позиция/Сетка/Вид/Появление; «+ Кусок» → «+ Элемент HUD»). S49-сцены-титул-сохранение завершена 2026-08-27: движок `f357fb3` (HUD-редактор стал мультисценовым: селектор «Карта (HUD)/Титул/Сохранение» + групповые тумблеры видимости HUD/Инвентарь/Крафт/Хотбар в баре под сценой (`_hiddenGroups`, `_groupHidden(id)` — только редактор, рендер и пик консультируются; `inv.hotbar` — своя группа «Хотбар», прочие `inv.*` — «Инвентарь», `craft.*` — «Крафт», всё остальное — «HUD»; кнопки: зачёркнутые/полупрозрачные = скрыто); сцена «Титул» (`_sceneDefs` из agonia-секции `title`: фон/спрайт титула/курсор только при Cursor Visible/Command Pos 1–9 как «x,y»-якоря — drag пишет строку «x,y», парсинг `(-?\d+)\s*,\s*(-?\d+)`, якорь создаётся только если ключ уже задан); сцена «Сохранение» (из секции `save`: декоративный фон + 13 якорей Rec/Play/Prev/Next/Stop/Back/Text/Diary/Cassette/Cover/Clip/Snap/List — X/Y-ключи, размеры из `Img Norm`-картинок); список/инспектор/`_strokeSelected`/`dragWindow`/`pickWindow` — пул по сцене (`_scene==='map' ? _windowDefs() : _sceneDefs()`); инспектор сценовых якорей — живые X/Y-поля через def.rect/apply (save.bg — только позиционируется декоративно); «Состояние игры» — только в Карты, в Титуле/Сохранении — заметка «условия не применяются»; `_scene`/`_hiddenGroups` инициализированы в КОНСТРУКТОРЕ (не в mount!) — тесты зовут API без монтирования).
- P1-точный-поиск-пути завершена 2026-08-27: движок `62c6284` (A\* в SuperDuperMovement_Addon переписан по золотым стандартам: проходимость = ТРИ слоя — тайл-флаги → collision mesh карты (точные формы: коллайдер ищущего в клетке через `Collider.intersect` с `polygonsWithinColliderList`) → **реальные хитбоксы объектов** (aabbox препятствия, раздутый на aabbox ищущего — Минковски-инфляция); статика (`!isDynamicMover`: нет `_amsSmartTarget`/`_moveRouteForcing`/`_moveType>0`, не игрок/фолловер) — жёсткий блок клеток, динамика — мягкая цена `Soft Cost Value` (+3: обходит толпу, но не тупит в единственном коридоре); цель погони exempt + очистка `Goal Exempt Radius` (2) вокруг цели; octile-эвристика (admissible при диагоналях √2 → гарантия кратчайшего пути), бинарная MinHeap (тай-брейк g↓), g-релаксация, без срезания углов, **best-effort** при недостижимости (путь к ближайшей достигнутой точке, не null; null только если не сдвинулись ни на шаг); новые параметры Hitbox Avoidance (on)/Soft Cost Value/Goal Exempt Radius — аддитивно, старые снапшоты работают; Debug Mode рисует точки путей всех ИИ поверх карты (цвет по типу поведения); **SuperDuperMovement экспортирует `window.Collider`** — контракт апстрима Altimit, ветка тонкой проверки SuperDuperDrop ожила; `__SDA_TEST`-хук; тесты `agonia-pathfinding.test.js` — 11 кейсов, сьют 177). Виз-проверка погони живьём при следующем плейтесте.
- S50-маркер-ГГ-невидим завершена 2026-08-27: движок `857f1e7` (фикс «ставлю ГГ на карту — маркер невидим в редакторе»: MapEditor включает `sortableChildren` у корневого контейнера карты → zIndex авторитетнее порядка добавления; `applyLayerState` раздаёт тайл-слоям z 10–90, stamps 900, events 950 (по label), highlights 960 — но контейнеру `startingPositions` zIndex никто не давал → 0 = похоронен под всеми тайл-слоями; регресс от Layers-panel коммита `1a5b3aa`, где по label сохранили только events. Фикс: `EventManager.initializeEventLayer` назначает zIndex САМ при создании контейнеров (events 950 / **startingPositions 955** / hover+selection 945 — устойчиво к таймингу, т.к. applyLayerState на загрузке карты выполняется ДО создания этих контейнеров); label-проход applyLayerState дополнительно страхует startingPositions при повторных вызовах из Layers Panel; заодно починен латентный баг выделения тайла в режиме событий (selection highlight тоже был под тайлами). Готча: top-level `class` в vm-скриптах НЕ вешается на global — тесты экспортируют явно `src + '\nthis.X = X;'`. Тесты `agonia-start-marker.test.js` — 3 кейса с PIXI-стабами (Container/Graphics/Text), сьют 180).
- P2-шаблоны-врагов завершена 2026-08-28: движок `5e64b94` (**враг = карточка БД, не 17 страниц руками**: карточка EnemyDatabase с `template='true'` разворачивается в полный 17-страничный автомат «бибы» на загрузке карты — экспансия в `Game_Map.setup` ДО создания событий, когда `$dataMap` уже загружен; скелет: покой(YurStealth-детектор)/тревога/трассер+лач A/бой+лач C/паника-погоня/контакт-прицел-приседание/remembergun-бегство/под-огнём/бегство/успокоение-сброс/рывок+меле/рана/таблица-урона/смерть; подстановки: спрайт+индекс, коллайдер-XML, tracerId/meleeId/dashName, пороги chase/cower по счётчику боя (var 59), таблица урона по оружию ГГ (var 17) со свитчем скрытного убийства, SE; **идемпотентно** (скелет пересобирается с карточки; порядок страниц фиксирован → селф-свитчи A/B/C/D в сейвах выживают), не-шаблонные события не тронуты; тег матчится тем же `match`, что у rule engine). Редактор: карточка «Бой → ИИ Врагов» + секции Шаблон/Внешний вид/Атаки/Урон (чекбокс template, спрайт+коллайдер, ссылки на карточки Боя и рывки, пороги, таблица урона), 🏷 в списке, blankEnemy с дефолтами. EventManager: ПКМ «Поставить Врага ▶» (i18n EN/RU `eventCtx.placeEnemy`) — подменю template-карточек → заглушка (1 страница: спрайт+коллайдер-комментарии). **Паритет доказан**: сгенерированные страницы байт-в-байт равны эталону (ивент 12 «тест боевки», прогон с реальными параметрами сайдкара + реальной заглушкой Map045). Миграция проекта: biba-карточка в AgoniaEngine.json (сенсоры из retired-снапшота verbatim: hp 100/scope global/attack 2/hearing 3/порог 3 + template-поля), ивент 12 → заглушка «ENEMY biba» (бэкап `Map045.biba-backup.json`); сайдкар ВСЕГДА вёл все 3 записи (box/biba/rat — сенсоры бибы работали), проектные файлы НЕ коммитить — смешаны с правками юзера. Тесты `agonia-enemy-templates.test.js` — 5 кейсов, сьют 185).
- S51-нпс-режим-и-свап-тулбара завершена 2026-08-28: движок `b3ce858` (**тулбар зависит от режима**: тайлам — инструменты рисования `tiles-only`, режиму событий — группа `events-only` 💡 Свет (тот же `toggle-light-mode`) + 👤 НПС (`toggle-npc-mode`); свап в обоих путях — тулбар-переключатель и меню mode-tiles; standalone 💡 прячется в режиме событий). НПС-режим = `EnemyInspectorManager` (паттерн LightManager): взаимоисключение с событиями/светом, заглушки врагов на карте в красных рамках (выбранный — синяя двойная, z 956), клик по врагу → плавающая панель справа с секциями: **Карточка** (тег/HP/scope + «Открыть в БД» → `reactor.openDatabase('enemyAI')`), **Визуал** (превью спрайта + спрайт/индекс/коллайдер), **Поведение** (сенсоры, пороги chase/cower, атаки с именами карточек Боя «№2 · Кувалда»), **Урон по врагу** (таблица оружие→урон, скрытное убийство, SE), **От чего зависит** (read-only: значения оружия ГГ, пороги счётчика боя, слух, свитчи 18/41/48, семантика селф-свитчей A–D), **На карте** (id/имя, X/Y-правка через undo-пайплайн, удалить), **Поставить врага** (список шаблонов → курсор copy → клик по карте = `createEnemyStub`). Правки карточек пишутся в сайдкар in-memory с грязь-маркером, 💾 — `saveAgonia`; плашка «меняет всех врагов вида». i18n RU/EN ~30 ключей; тесты `agonia-npc-inspector.test.js` — 7 кейсов, сьют 192.
- S51b-стейт-мастер-режимов завершена 2026-08-28: движок `006d664` (фикс «меню живёт своей жизнью»: НПС/Свет входили через ПОЛНЫЙ выход из режима событий → тулбар прыгал на инструменты рисования (кнопка НПС исчезала — режим не выключить), иконка EV→TL, побочка тайлов (setupMapInteraction/refreshBackgroundGrid/отдим слоёв) ломала затемнение в режиме света. Фикс: **`setEditingMode('tiles'|'events'|'light'|'npc')`** — единый владелец переходов; Свет/НПС — ПОДРЕЖИМЫ контекста событий (тулбар держит events-группу и иконку EV, выключение подрежима → в события; тайловые побочки только при реальном пересечении границы tiles↔events); `_syncModeChrome` — группы + иконка + active-состояния подрежимов + источник undo/redo + дим слоёв (событ. = рид-онли дим 0.5, light/npc — полный яркий); standalone-фонарик УДАЛЁН из тайлового тулбара — `mode-light-btn` теперь на кнопке events-группы; перезагрузка карты восстанавливает полный режим (events/light/npc + хром), не только eventMode; `ensureEventMode` через стейт-машину; guard'ы UIManager (Ctrl+Z/Y/C/X/V, Delete, стрелки, undo/redo-меню) решают по КОНТЕКСТУ событий (`getEditingMode`-коллбек с eventMode-фоллбеком) — раньше Delete в light/npc проваливался в удаление выбранной КАРТЫ; NPC-режим сбрасывает курсор placement и ставит default. Сьют 192.
- S52-поведенческий-конструктор завершена 2026-08-28: движок `0e05e86` (**скелет подчиняется карточке**: `disposition` aggressive (дефолт = бит-в-бит прежние 17 страниц, проверено по фикстуре бибы) | peaceful — paciфик: выпадают атакные страницы (трассер-выстрел P2, атака рывком+удар P12), остаются тревога/бой-флаг/раны/урон/смерть; тумблеры способностей: `canPanic` → минус P6/P7, `rememberGun` → минус P8, `canFlee` → минус P9/P10 — селф-свитчи живут в УСЛОВИЯХ страниц, не в индексах → сейвы совместимы; скорости `speedCalm` (3: покой/тревога/выстрел/рана) / `speedCombat` (4: боевые) из карточки; `dashName` три-стейт: undefined = дефолт «РывокВрага», '' = без рывка; zona-страница только злому с хоть одной атакой). НПС-панель 400px + секции: **Характер** (селект Злой/Мирный с пояснениями), **Способности** (Пугается/Убегает/Помнит оружие), **Скорость** (в покое/в бою), **Флаги ИИ** — справочник-чипы: сенсоры (zona/contact/hearing/scope/gun/melee/dead/light_*, движок считает всегда) + классика (combat/calm/warning/panic/flee/remembergun/shot/loch/wound), активные подсвечены по факту включённых страниц/тумблеров, строка-сводка атак; customRules остаются в БД. БД-карточка «ИИ Врагов» — те же поля в синхроне (Секция «Характер» в Шаблоне). Тесты: +4 скелетных (мирный/тумблеры/скорости/P12-без-атак), панельный расширен — сьют 196.
- P3-арсенал завершена 2026-08-28: движок `3c3ccf3` (**центральный справочник оружий водит урон и страх**: коллекция `Weapon List` живёт ВНУТРИ секции enemies — мост доставляет её в SuperDuperEnemies без правок реестра; карточка: name/varValue (var 17 — связь со скином Спрайтера)/type (melee-ranged-tool-light)/damage/sneakKill (вне боя — насмерть)/noise (справочно)/se). «Бой → Оружие» — 6-я кнопка, classicApi `weapons`. Скелет ест Арсенал: **P14 генерируется из карточек оружий** — вложенная if-цепочка по оружию (овverride врага `damageOverrides` > урон оружия, SE оружия, sneak-ветка по свитчу «вне боя», wound), fallback кулаки `damageFists`; без Арсенала — легаси-таблица бит-в-бит (проверено фикстурой). **Страх**: `fearedWeapons` (строка varValue через запятую) на P6/P7/P8 заменяет `<Gun>`-сенсор на сгенерированные var-17 else-if цепочки (111/412 сбалансированы, погоня цела); пусто = глобальный GunCondition. **Панель НПС переработана по фидбеку**: 620px, ДВЕ КОЛОНКИ, без скролла — слева «кто он» (Характер + чекбоксы страха из Арсенала, Способности, Скорость, Визуал), справа «как дерётся» (Поведение, Урон — компактная таблица Арсенала с оверрайд-инпутами, Флаги ИИ, На карте, Поставить); секция «Карточка» свёрнута в строку шапки (тег + инлайн HP + ⓘ + БД), текстовый блок «От чего зависит» удалён (справочник флагов покрывает). Проект: сид Лом (var 2, −20 + sneakKill — паритет бибы на HP 100) и Ствол (var 37, −100). Тесты: +4 плагин / +2 панель, сьют 199, смоук 31/31.
- P3b-панель-влезает+пикер завершена 2026-08-28: движок `50aaace` (фикс «половина второго ряда уходит за край, текст не читаем»: маржины секций/строк ужаты, Флаги-справочник + На карте/Поставить вынесены из двух колонок в **нижнюю полосу на всю ширину** (`grid-column:1 / -1`, с верхней границей) — высота панели теперь по короткой колонке; `max-width:calc(100% - 64px)` — на узком контейнере сжимается, не вылезая за левый край. **Визуал без превью**: картинка и поле индекса убраны — одна строка «Спрайт» + кнопка «…» открывает двухшаговый пикер Спрайтера (файл листа → ячейка 4×2, $-листы = одна ячейка), пишущий в карточку сразу и имя, и индекс; мёртвые `_spriteUrl`/`_req` удалены. Сьют 199).
- P3c-грид-блоуаут завершена 2026-08-28: движок `1e1489d` (фикс «большая часть второго ряда за пределами края МЕНЮ»: все `1fr`-треки панели НПС (колонки тела, Способности/Скорость/Поведение, нижняя полоса) были без `min-width:0` — контент с собственной шириной (длинные опции селекта характера, подписи страха, таблица урона, строка атак) распирал треки и выдавливал вторую колонку за правую границу панели — тот же класс бага, что S37b. Фикс: треки → `minmax(0,1fr)`, обёртки колонок/полосы + `_row`/`_chk`/`_chkFeared`/селект → `min-width:0`, строка атак → `overflow-wrap:anywhere`; тест ассертит shrinkable-разметку. Сьют 199).
- P3d-инпуты-нечитаемы завершена 2026-08-28: движок `54cf5eb` (фикс «окна радиуса атаки/порога шума/слуха столь малы, что нечитаемы»: внутренние двухколоночные гриды «Поведение»/«Скорость» делили уже ПОЛОВИННУЮ колонку панели ещё на два → ячейка ~140px, метка 118px → инпут ~15px. Фикс: поля — полноширинные одноколоночные строки (~165px инпут; правая колонка остаётся короче левой — высота панели не растёт); всем карточным инпутам `min-width:52px` как пол от схлопывания. Сьют 199).
- P5-карточки-состояний завершена 2026-08-28: движок `d89cc1b` (**карточка состояний: флаг → графика + звук**: поля `stateGraphics`/`stateSounds` (JSON по фикс-набору покой/тревога/бой/паника/бегство/атака/урон/смерть) — компилятор подставляет спрайт состояния в его страницы (пусто = основной; смерть без графики = пустой спрайт как в оригинале) и играет SE на входе ТОЛЬКО однократных страниц (тревога P1 / атака P2+P12 / урон P14 (stateSounds.hurt перекрывает damageSE-fallback) / смерть P15) — циклические не спамят; легаси damageSE работает. **Шаги**: SuperDuperSteps парсит `<step_se:VOL>` (0–150% поверх дистанционного фейда — фейд УЖЕ был, радиус 8 тайлов); `createEnemyStub` дописывает тег шагов в каждую заглушку с `stepVolume` карточки (раньше поставленные враги ТЕРЯЛИ шаги — только ручная биба имела). **БД-карточка дружелеет**: fearedWeapons = чекбоксы Арсенала, damageOverrides = таблица «оружие→урон» с оверрайд-инпутами (сырой JSON убран), сетка «Состояния» с мини-превью 24×32 + пикер Спрайтера + SE-поля; +stepVolume. **Панель НПС**: спавн-палитра — вечно-видимая полоса чипов НАД колонками (нижнюю секцию заменила — была нескрываемой), компактные «Состояния» (клик по строке = пикер графики, SE-инпут), поле громкости шагов; `openDatabase` полностью гасит НПС-режим. Дефолт скелета бит-в-бит (фикстура бибы); тесты +3 (графика состояний/звуки однократных+легаси/тег заглушки) + расширения панельного — сьют 202, смоук 31/31. Готча: файлы плагинов — CRLF, патч-скрипты должны нормализовать `\r\n`).
- P6-превью-и-палитра завершена 2026-08-28: движок `0cae1f6` (**живое превью в БД + спавн-палитра в сайдбаре**): карточка врага (Бой → ИИ Врагов) открывается АНИМИРОВАННЫМ превью как ПЕРВОЙ секцией — плеер Спрайтера (96×128, 3-кадровая ходьба 8fps, выбор направления, ⏸/▶) через собственный инстанс DatabaseSpriterEditor у редактора врагов (пул игроков отдельный от вкладки Спрайтера); fake-entry с getter'ом `Visuals` живо перезагружает картинку при правке spriteName/spriteIndex; рецикл тегом при перерисовке. **Спавн = сайдбар-палитра**: секция `#enemy-palette-section` на месте тайлсетной — в НПС-режиме ЗАМЕНЯЕТ палитру тайлсета (свап в `_syncModeChrome`; выход восстанавливает тайлсет и глушит плееры); грид живых мини-карточек (анимированные спрайты + имя + HP) по всем template-записям — клик взводит постановку (рамка + курсор copy), повторный снимает, клик по карте = `createEnemyStub`; идентичность шаблонов по match-тегу (getTemplates репарсит объекты — сравнение по ссылке ломало disarming). Спавн-полоса из шапки НПС-панели удалена (заменена палитрой). Тесты: палитра (карточки/взвод-снятие), БД-превью первым элементом (canvas+селект направления+«Живое превью») — сьют 204, смоук 31/31).
- P7-пять-карточек+инструмент+контекст-сайдбар завершена 2026-08-28: движок `4592e71` (**БД-карточка врага открывается РЯДОМ ИЗ ПЯТИ карточек графики** — Основная/Тревога/Атака/Урон/Смерть, каждая анимированный мини-плеер с подписью-использованием; незаданные — дефолтная графика с пометкой «по умолчанию»; клик = двухшаговый пикер, карточка ряда обновляется на месте (снапшот-детектор плеера живо перезагружает через Visuals-getter)). **Инструмент «Событие»** — первая иконка events-группы (аналог карандаша): одиночный клик по пустой клетке СОЗДАЁТ событие (редактор с isNew), по событию — выбор/драг; кнопка тумблер (`toggle-event-tool`) выключает в режим «только выбор», active-синк в `_syncModeChrome` учитывает `eventManager.eventToolActive`. **Сайдбар полностью контекстный** (секция переименована в `context-palette-section`): события → список ивентов карты (мини-превью с object-position по ячейке, имя, координаты; клик = `selectEventById`+подсветка, двойной = редактор), свет → список источников (цветной свотч, тип/пресет, радиус, active, координаты; клик = `_select(uid)` через хуки `_getLights`/`_onSelectLight` из main.js), враги → палитра P6, рисование → тайлсет. Тесты: ряд 5 карточек (подписи/«по умолчанию»/канвасы), палитра событий (выбор/dbl), палитра света (uid), инструмент (создание по пустой/выбор по событию/выкл) — сьют 207, смоук 31/31.
- P8-две-секции+без-скобок завершена 2026-08-28: движок `57e54ff` (**палитра режима событий — ДВЕ секции**: «Виды врагов» — карточки БД живыми мини-плеерами через общий билдер `_appendEnemyTemplateGrid` (клик взводит постановку ПРЯМО в режиме событий); «События карты» — список ивентов с превью, клик выбирает, dbl — редактор. Постановка в режиме событий: `handleMapPointerDown` проверяет взведённый вид ДО ветки двойного клика — клик по пустой клетке ставит заглушку через `createEnemyStub`, снимает постановку и перестраивает палитру (хуки `_getArmedEnemyTemplate`/`_disarmEnemyTemplate` из main.js). **Правило юзера закреплено: в текстах интерфейса НИКАКИХ дополнений в скобках** — сметены все RU+EN: «Враги (НПС)»→«Враги», «Враги (спавн)»→«Виды врагов», «Состояния (графика + звук)»→«Состояния», «Скорость (MV 1–6)»→«Скорость», «Без оружия (кулаки)»→«Без оружия», «SE (fallback)»→«Общий звук урона», пороги/подсказки перефразированы без скобок). Тесты: двухсекционная палитра (заголовки/взвод/строки событий), постановка врага в режиме событий (ставит+снимает, обычное создание восстанавливается) — сьют 208, смоук 31/31.
- P9-единые-двухсекционные-палитры завершена 2026-08-28: движок `42dcc91` (**у каждого режима — свой контент, единая двухсекционная форма «Шаблоны» / «На карте»** — откат P8-мерджа врагов в палитру событий по указанию юзера: «ивенты в ивентах, враги во врагах». События: «Шаблоны» = общие события БД (`_getCommonEvents` через `databaseManager.getCommonEvents`) — клик взводит, клик по карте = `createCommonEventStub` (событие с командой вызова 117, хуки `_getArmedCommonEvent`/`_disarmCommonEvent`); «На карте» = список событий. Свет: «Шаблоны» = библиотека панели Освещения (`getLibrary`, свотч+имя+радиус) — клик взводит `lightManager._pendingTemplate` + `enterPlacement`, клик по карте = `addSynthetic(tx,ty,шаблон)` (сигнатура расширена аддитивно, свойства поверх дефолтов, потребляется и чистится); «На карте» = источники. Враги: «Шаблоны» = карточки БД (грид `_appendEnemyTemplateGrid`); «На карте» = НОВАЯ секция — заглушки врагов карты (мини-превью + тег + имя + координаты; клик = `select` + рамка-подсветка). Заголовки секций единые «Шаблоны»/«На карте» (EN Templates/On map), заголовки палитр по режиму События/Освещение/Враги, `_rebuildContextPalette`-хелдер в main.js; без скобок. Тесты: палитры трёх режимов (секции/строки/взводы/выбор стаба), постановка общего события (117 + параметр + disarm) — сьют 210, смоук 31/31).
- P10-вкладки+шаблоны-из-существующего завершена 2026-08-28: движок `0b492d8` (**переключатель секций + шаблоны создаются ИЗ существующего**): каждая контекст-палитра открывается с ВКЛАТКАМИ «Шаблоны» / «На карте» (кнопки в стиле Map Tree, выбор помнится per-режим через `_paletteTab`, дефолт «На карте»). **Шаблоны событий = сохранённые копии реальных событий, НЕ общие события** (интерпретация P9 откачена): ПКМ по событию → «Сохранить как шаблон» → глубокая копия в `AgoniaEngine.json['eventTemplates']` (авто-имя из имени события + счётчик дублей, персист дебаунсом через `saveAgonia`); вкладка шаблонов — строки с мини-превью, клик взводит постановку → клик по карте ставит глубокую копию с новым id (`placeEventFromTemplate` в том же конвейере до ветки двойного клика); ✕ удаляет шаблон. **Свет — тоже из существующего**: ☆ на каждой строке «На карте» сохраняет источник в Библиотеку панели Освещения (авто-имя цвет+радиус, дедуп) через `_onSaveLightTemplate` — одно хранилище с вкладкой Библиотека. Код общих событий вычищен полностью (`createCommonEventStub`, секция, хуки). Тесты переписаны: вкладки с памятью, глубокие копии+авто-имена, постановка копии, ☆, вкладки врагов — сьют 213, смоук 31/31.
- P11-вкладки-дом-кит+зазор завершена 2026-08-28: движок `07227af` (вкладки «Шаблоны»/«На карте» — настоящие кнопки дом-кита `agonia-btn` с акцентной рамкой+жирным на активной, вместо блёклых инлайн-дивов; регистр нормальный вместо капса RU/EN. Зазор другого цвета при переключении Тайлы→События = **осиротевший resize-handle тайлсетной секции** (свап прятал секцию, но 4px хэндл оставался block; у контекст-секции своего хэндла не было) — контекст-секция получила собственный `resize-handle`, свап вызывает `sidebarResizer.updateHandleVisibility()` — хэндлы всегда соответствуют фактическому display, палитра ещё и ресайзится. Сьют 213, смоук 31/31).
- P12-пояснения+тихий-инструмент+тьма завершена 2026-08-28: движок `c382714` (**правило юзера закреплено: в UI НИКАКИХ поясняющих подписей-инструкций** — вырезаны хинты палитр «Клик — выбрать · двойной клик — редактор · ПКМ — в шаблоны» и абзацы-подсказки НПС-панели «Состояния»/«Урон»/«Страх»; интерфейс — только имена и контролы). **Инструмент «Событие» больше не врывается редактором**: одиночный клик по пустой клетке ставит событие ТИХО (`createNewEvent(opts.silent)`: saveState → в карту → renderEvents, без диалога), редактор — только по двойному клику, как раньше. **Режим света показывает тьму**: вход в свет авто-включает плейтест-превью (multiply-маска), если юзер не включил его сам через ☀; выход восстанавливает прежнее состояние превью. Тесты: тихая постановка/дбл-редактор/выкл-инструмент; сьют 213, смоук 31/31.
- P13-тьма-жизненный-цикл завершена 2026-08-29: движок `a81d0d2` (фикс «тайлы затемнения в режиме света не показываются» — корень НЕ в рендере: живой NW-эксперимент с тем же Pixi 8.14 доказал, что multiply-маска работает идеально; vm-симуляция с реальным патчем проекта показала корректное создание спрайта. **Дыры были в ЖИЗНЕННОМ ЦИКЛЕ превью**: (1) `setCurrentMap` уничтожал устаревший спрайт при каждой смене карты — и никто не пересоздавал его: previewOn висел true без спрайта → смена карты в режиме света навсегда теряла тьму; (2) блок восстановления после загрузки карты зовёт `setLightMode(true)` напрямую, минуя `setEditingMode` — P12-авто-превью срабатывает только при previewOn=false, и «залипший» true пропускал рендер при каждом повторном входе; (3) `initializeLightLayer` пересоздаёт контейнер при смене карты/проекта с той же мёртвой маской. Фиксы: `setCurrentMap` сразу пересоздаёт превью под новые размеры; `setLightMode(true)` рендерит при previewOn (покрывает restore-путь и stale-флаг); `initializeLightLayer` рендерит после перепарентинга. Проверено в vm-харнессе на реальных картах проекта: переход 45→6 пересоздаёт маску 2880×3360 → 912×624 с маркерами. Сьют 213, смоук 31/31).
- P14-клик-ничего+тьма-в-ядре завершена 2026-08-29: движок `026e04f` (**одиночный клик в режиме событий больше НИЧЕГО не создаёт** — инструмент «клик-создание» P7/P12 полностью откачен: создание событий — двойной клик или ПКМ-меню, одиночный клик — только выбор/драг как всегда; иконка инструмента «Событие» осталась как индикатор-аналог карандаша без семантики создания, `eventToolActive`-ветка удалена из `handleMapPointerDown`). **Тьма включается в самом `setLightMode(true)`**: `previewOn = true` + `renderPreview` — ЛЮБОЙ путь входа в режим света (кнопка, меню, восстановление после загрузки карты) показывает затемнённую карту; ☀-кнопка синхронится на входе/выходе, выход из режима возвращает состояние превью до входа (раньше авто-превью жил только в обёртке `setEditingMode` — путь меню/restore его обходил). Тесты: одиночный клик ничего не создаёт, двойной — редактор. Сьют 213, смоук 31/31).
- P15-регионы+выделение завершена 2026-08-29: движок `c362148` (**тайлы затемнения = регионные стены SDLight — корень: `_loadBlockedRegions` читал ТОЛЬКО `js/plugins.js`, а у мостовых проектов манифест ПУСТ** (`$plugins = []`) — SDLight живёт в `project.rpgreactor` (engineModules/retiredPlugins → `.parameters`), поэтому `_blockedRegions` всегда был `{}` и `_renderRegionBlocksOnCtx` выходил сразу: тёмные стены-регионы никогда не рисовались в превью (свет и общая тьма — другая подсистема маски, они работали). Фикс: нет SDLight в манифесте → параметры из `project.rpgreactor` (сначала engineModules, затем retiredPlugins); пустые значения — через PROJECT_DEFAULTS (регионы 8,1,11–14, матчинг собственного фоллбека игры); plugins.js приоритетнее, когда запись есть. **Выделение клеток**: `handleMapPointerDown` зовёт `selectTile` (рисует циановую рамку) и следом `selectEvent(eventAtPos)` — который БЕЗУСЛОВНО прятал `selectionHighlight` («спрайт-бордер теперь визуал»), даже при `null` — каждый клик по пустой клетке рисовал и мгновенно гасил подсветку. Фикс: прятать только при реальном событии. Тесты `agonia-light-regions.test.js` (пустой манифест → регионы из rpgreactor+дефолты; явные перекрывают; plugins.js приоритетнее) + выделение в start-marker — сьют 216, смоук 31/31.
- P16-мебель-у-цели завершена 2026-08-29: движок `5a38956` (фикс «враг периодически не считывает свой хитбокс и хитбокс стула, застревая рядом»: (1) **Goal Exempt Radius больше не стирает статику** — радиус задуман для толпы у игрока, но вытирал и мебель в 2 клетках от цели → A* строил путь сквозь стул, физика блокировала, враг буксовал; exempt теперь только для динамики (другие ИИ/фолловеры), статика — стена всегда, сама цель по-прежнему пропускается; (2) **LOS-шаг 0.5 → 0.25** — прямой шорткат и сплайн вейпоинтов перепрыгивали узкие коллайдеры мебели на диагоналях (непрерывное тело чиркало угол → физика блокировала); (3) **умный anti-stuck**: при застревании немедленный re-plan (refresh=0) + одноразовый блэклист буксующей клетки (жёсткая в следующем поиске, взводится в updateSmartPathLogic, пушится buildPathContext, потребляется findPath) + пинок ПЕРПЕНДИКУЛЯРНО желаемому вектору (скольжение вдоль стены) вместо рандома; бонус: порог прибытия 0.3 у вейпоинта рядом с жёстким препятствием (`_amsPathCtx` едет с путём, LOS-путь его чистит) — враг доходит до безопасного центра клетки. Тесты: статик у цели блокирует/динамика exempt, блэклист одноразовый с обходом, LOS-плотность 0.25 + диагональная зона стула — сьют 218, смоук 31/31. Живой гейт — плейтест погони сквозь мебельную комнату.)
- P17-дебаг-маршрута завершена 2026-08-29: движок `4922f72` (визуализация построения маршрута при Debug Mode: сплошная полилиния между вейпоинтами — Брезенхэм fillRect 1px, у MV Bitmap нет drawLine — плюс линия от врага к первому вейпоинту, поверх точек и кольца цели, цвета по типу поведения; в проекте `Debug Mode = true` в retired-снапшоте project.rpgreactor — мост доставляет системному модулю; перерисовка раз в 10 кадров, guard 2048 сегментов. Сьют 218).
- P17c-дебаг-невидим завершена 2026-08-29: движок `3440590` (фикс «нихуя не рисуется» — два столбленных бага найдены живыми NW-зондами + GPU-екстрактом спрайта: (1) **КОРЕНЬ: MV `Sprite.opacity` — шкала 0–255, НЕ 0–1** — оверлей создавался с `opacity = 0.9` → worldAlpha 0.0035: канвас 499 пикселей пути, GPU 0, юзер никогда не видел ни точки (теперь 230); (2) перерисовка жила в алиасе `Scene_Map.update` (хрупок против перепривязок других плагинов) и **у простых Sprite никто не зовёт `Bitmap.checkDirty`** (только Window/TilingSprite) → WebGL-текстура канвас-битмапа не перезаливалась — оверлей теперь `Sprite_PathDebug`-подкласс с собственным `update()` (ядро MV зовёт update детей каждый кадр мимо любых алиасов) + `checkDirty()` + `_baseTexture.update()`. Доказательство: зонд в игре включает погоню, ждёт 3 цикла, естрактит рендер: GPU 241 красный пиксель при worldAlpha 0.90 (было 0). Диагностика через инжект rr-*.js в index.html (автостарт новой игры, дампы в файл) — артефакты вычищены. Готча на будущее: MV opacity 0–255; canvas-bitmap Sprite требует ручного checkDirty. Сьют 218, смоук 31/31.)
- P18-дебаг-инфраструктура завершена 2026-08-29: движок `0321e16` (**постоянный дебаг-инструментарий**: кнопка 🐞 в тулбаре правее ▶ — `debugPlaytest()` = полный оркестратор плейтеста, но спавн с env `RPGREACTOR_DEBUG=1`; новый плагин каталога **`AgoniaDebugKit`** — 10-й системный модуль моста, при обычном запуске СПИТ (выходит до любых алиасов), от env или `?debug` в URL просыпается: автостарт «новая игра» без титула, бейдж DEBUG, **F8** принудительная погоня всех умных ИИ (свитч Force Chase Switch — exemption сюжетных), **F9** дамп диагностики в `debug/diagnostics-<map>-<frame>.json` (карта/события/цели/маршруты/препятствия контекста/перехваченные ошибки), **F10** тумблер оверлея маршрутов; хоткеи на document напрямую (MV Input не мапит F-клавиши). Аддон поиска пути включает отрисовку маршрутов от того же env — без правки конфига проекта. Проект repatch (реестр моста несёт кит). Тесты: env-флаг аддона (debugActive), сон/пробуждение кита (маркер `__DebugKitActive` + алиас `_dkPatched`), счётчики системных модулей 9→10 — сьют 220, смоук 31/31.)
- P19-зум-оверлея завершена 2026-08-29: движок `199ecf7` (фикс «отрисовка пути не учитывает зум»: MV-зум живёт на ВСЁМ `Spriteset_Map` — `Spriteset_Base.updatePosition` ставит `scale = zoomScale` + сдвиг `-zoomX*(scale-1) + shake`, а оверлей маршрутов добавлен на `Scene_Map` МИМО спрайтсета → точки рисовались в нескейленных координатах и уплывали до ~150px при 1.6×. Фикс: каждая мировая точка проходит `pathDebugXform(scene._spriteset)` → `round(local*k)+origin` (хелпер ПОДНЯТ выше дебаг-блока — `__SDA_TEST.pathDebugXform` доступен без Debug Mode; тест на идентичность/скейл/zero-fallback). Живое доказательство в NW: статичный wander-таргет с огромным таймером (аддон зануляет скорость и выходит каждый кадр — детерминированная сцена без скролла) при зум 1.6 — пиксель ровно на трансформированной точке (minDxf 0), нескейленная — 153px в стороне. Сьют 221, смоук 31/31.)
- P20-хитбоксы-в-дебаге+инвариант-пути завершена 2026-08-29: движок `bac4401` (**дебаг показывает твёрдые предметы, путь через них не прокладывается**). Оверлей маршрутов рисует коллайдеры всех персонажей: статика/мебель (жёсткие стены A*) — красные рамки с полупрозрачной заливкой, динамика (мягкая цена) — синие рамки; это ровно слой-3-боксы `cellPenalty`, зум-трансформация как у вейпоинтов (P19) через corner-маппинг (углы — абсолютные тайловые координаты, без +tw/2). `collectHitboxes()` — сырые AABB per-character; F9-дамп кита несёт счётчики хитбоксов. **Инвариант пути**: после клеточного A* сегментная валидация семплит каждую линию вейпоинт→вейпоинт с шагом 0.25 против инфляционных hard-AABB — тонкий коллайдер может «прошить» диагональ между центрами свободных клеток, а финальная дробная точка чиркать зону мебели у цели; пересечение → клетки пересечения жёстко блокируются (`ctx.extraBlocks`) + ОДИН пересбор в обход (дальше best-effort, anti-stuck — физический бэкстоп). Экспорт `__SDA_TEST`: hardSegmentBlocked/collectHitboxes/overlayDraw (redrawPathDebug блочно-скоупен — мостик `sdaOverlayDraw`). **Готча MV (живой прогон)**: у Bitmap НЕТ `strokeRect` — TypeError молча глотался try/catch оверлея и НЕ рисовалось ничего (даже путь); рамки = 4 fillRect по 1px. Живое доказательство на мебельной карте при зум 1.6: красный пиксель ровно на трансформированном угле бокса (minCorner 0; 3114 hard px, 757 dynamic px), путь minDxf 6≤24; тесты: «нитка» на диагонали (предусловия задокументированы) + классификация + зум-математика оверлея — сьют 225, смоук 31/31.)
- P22-три-бага-боя завершена 2026-08-29: движок `fbb5064` (**(1) вейпоинты в клетках твёрдых ивентов / враг долбится сквозь мебель**: Минковски-AABB препятствий — интервал для ЦЕНТРА ищущего, а cellPenalty/hardSegmentBlocked/LOS-семплы/steering проверяли его как бокс против бокса ПОВТОРНО → зона блокировки сдвинута на полклетки влево-вверх: маленькие/высокие коллайдеры (стул y .45–.75) оставляли свою клетку СВОБОДНОЙ и фантомно блокировали клетку сверху — полнотайловая мебель это маскировала. Все четыре потребителя переведены на «центр-в-интервале» (x>x1 && x<x2...), _amsNearHard следом; **статик с явным коллайдером страницы (`<collider>`) — навигационная стена даже при приорите «ниже игрока»** (физика его не толкает, но сквозь стул визуально проходить нельзя — collidableWith больше не фильтрует); collectHitboxes зеркалит предикат. (2) герой не умирал при 0 HP: красивая смерть (помехи noise/scanline/chroma, тряска, BGS, меню чекпоинт/сейв/титул) в SuperDuperGameOver только ПЕРЕХВАТЫВАЕТ SceneManager.goto(Scene_Gameover) — триггером был BattleManager.processDefeat, ампутированный с боем; ватчера HP не было нигде. Добавлен ватчер смерти на карте в сам плагин (алиас Game_Player.update: hp<=0 лидера → goto(Scene_Gameover), re-arm guard). (3) лаг дебаг-оверлея**: редроу каждый кадр вместо каждого 10-го (замер 0.97мс полного редроу). Живые пробы: геометрия юзера (герой ниже пары коллайдеров, биба правее) — 0 вейпоинтов на твёрдых клетках в обеих фазах (prio=1 и forced prio=0), обход строится; смерть — setHp(0) на карте → помехи → Scene_SuperDuperGameOver. Тесты: регресс «маленький высокий коллайдер блокирует СВОЮ клетку, не клетку сверху» + steering-геометрия пересчитана под честную симметричную формулу (старый «поворот» был фантомной асимметрией сдвинутого интервала) — сьют 228, смоук 31/31. Готча: замер зависшего NW-диагностического прогона был ФЛУКОМ до создания спрайта оверлея — каждокадровый редроу безопасен; сплэш-заставка ест ~16с — пробы должны ждать Scene_Map дольше 20с.)
- P23-сквозная-погоня завершена 2026-08-30: движок `2271ae8` (**враги преследуют героя между локациями через двери**; дебаг-оверлей по умолчанию СКРЫТ — F10 в дебаг-запуске тумблер). Механика: хук `Game_Player.performTransfer` собирает всех событий с активной погоней при уходе героя в ПРИЗРАКИ — реестр на `$gameSystem._sdPursuit` (выживает в сейвах): {match-тег, селф-свитчи фазы боя, HP, нога-дверь с точным назначением, ETA = путь до двери + транзит, тайм-аут интереса}. Тик на `Game_Map.update`: нога совпала с картой игрока и ETA истёк → МАТЕРИАЛИЗАЦИЯ — заглушка того же вида телепортируется к двери прибытия, либо (нет заглушки) динамический инжект `Game_Event` со скелетом из карточки (`SDE_API.buildTemplatePages/registerInjectedEvent/setEventDataHp/getMatchTag`), с `DataManager.extractMetadata`, ручным `Sprite_Character`, восстановлением фазы/HP и реармом погони. Игрок ушёл дальше — призрак едет по статическому графу дверей (BFS поверх `data/Map*.json` через NW fs: события с кодом 201, триггер 0/1, прямой режим); тупик — ждёт у двери; интерес 120с — сдаётся; кап 4 призрака. Параметры: Cross-Map Pursuit / Pursuit Limit / Transit Frames / Interest Timeout / Door Search Radius. **Уроки (живыми пробами):** (1) активные страницы событий ПЕРЕЗАПИСЫВАЮТСЯ в рантайме чужими плагинами (скелеты P2 добавляют свои 201, aex-зоны меняют триггеры) — источник истины только статический граф из файлов данных, рантайм даёт лишь живые координаты двери; (2) у MV Game_Player НЕТ `_mapId` — симулируемая карта всегда карта игрока, «враг на карте без игрока» не существует, вся межкарточность идёт через призраков; (3) инжект события требует `meta` (SuperDuperMovement читает `dataEvent.meta.collider`). Живое доказательство: биба на 45-й, герой уходит через ev7 → призрак {leg map129@6,7} → материализован на 129-й инжект-событием #39 (спрайт 21→22) и сразу гонится. Тесты `agonia-crossmap-pursuit.test.js` (парсинг дверей/BFS/снапшот/лимит/тик/ожидание/интерес/материализация) — сьют 234, смоук 31/31.)
- P24-YurStealth-краш-переноса завершена 2026-08-30: движок `09b9030` (фикс «при перемещении на локацию TypeError: Cannot read properties of undefined (reading '_x') at YuryolStealth.js:92»: модульный список вотчеров YuryolStealth привязан к событиям КОНКРЕТНОЙ карты, но никогда не чистился — после переноса `$gameMap.event(id)` старой карты = undefined и проверка дистанции роняла `Scene_Map.update` каждый кадр. Фиксы: алиас `Game_Map.setup` сбрасывает список при каждой смене карты; цикл координат выбрасывает записи без события (splice, без throw) — даже сейвы посреди переноса самолечатся; перерегистрация того же id ЗАМЕНЯЕТ запись, а не дублирует (скелет P2 перезапускает детектор на каждом реарме страницы — список раздувался бесконечно). Тесты `agonia-yuryol-stealth.test.js` (4): живая проба — 3с на 45-й с детектором бибы → перенос на 129-ю → 0 ошибок страницы. Сьют 238, смоук 31/31.)
- P25-карточки-состояний-врага завершена 2026-08-30: движок `6d2f915` (**карточки-состояния = главный редактор графики и звука вида**; по репорту юзера «пять карточек только показывают графику, менять нельзя, внизу графика+звук — перенести в карточки»). Корень «нельзя менять»: геттер превью P7 читал `gfx[key].spriteName`, а рантайм-формат (и пикер) — `{name,index}` → своя графика рендерилась пустой. Ряд переделан: **ВОСЕМЬ карточек** (Основная/Тревога/Бой/Паника/Бегство/Атака/Урон/Смерть — все состояния скелета), каждая — живой мини-плеер (геттер перечитывает stateGraphics при каждом доступе → выбор пикера виден сразу), клик = двухшаговый пикер Спрайтера, SE-поле под превью пишет stateSounds, маркер «по умолчанию»/акцентная рамка обновляются на месте. Нижняя секция «Состояния (графика + звук)» УДАЛЕНА (перенесена в карточки); юзейдж-подписи-инструкции вырезаны (P12). Смоук-кейс: 8 карточек с SE, чтение `{name,index}` (alert прячет маркер), SE-коммит персистит, нижней секции нет; npc-inspector-тест переписан с пятикарточного контракта на восьмикарточный — сьют 238, смоук 32/32.)
- P26-звук-состояний-SE-BGS-радиус завершена 2026-08-30: движок `96f1ad1` (по репорту юзера «нажимаю на SE — не могу выбрать BGS, который будет издавать враг; нужен радиус звука и громкость; карточки больше»). **Редактор**: карточки-состояния укрупнены (сетка 150px); на каждой из восьми — полный звуковой блок: селект типа **SE/BGS**, файл через пикер-модал со списком `audio/se|bgs` проекта (кнопка «…», плюс свободный ввод), громкость 0–100 и радиус слышимости — персист `stateSounds[key] = {name, kind, volume, radius}` (легаси-строки читаются нормализованно; пустое имя = записи нет). **Рантайм** (`sdeSound`): объект с радиусом > 0 → **натив 728** (позиционный OcRam AEX на якоре-событии: дистанция/радиус/фейд/автопан) для SE и BGS; радиус 0 → обычные 250/261 с громкостью; строки — легаси SE@90 бит-в-бит; пустое имя → ничего (колл-сайты фильтруют null). Тесты: скелет (728 с kind/vol/radius / 250 с vol / пустое имя / легаси-строка), контракт карточек в npc-inspector переписан (8 селектов, 8 файлов, 16 чисел, 150px, персист объектов) — сьют 239, смоук 32/32.)
- P27-карусель-BGS-компакт завершена 2026-08-30: движок `2a14099` (по репорту юзера: «SE убери, оставь только BGS; карточки в одну линию каруселью; громкость и второе поле — в том же меню, где выбираешь музыку; HP ток числом; инфу ниже компактнее»). **Редактор**: карточки-состояния — ОДНА ЛИНИЯ-карусель (карточки 150px flex-basis, горизонтальный скролл, стрелки ◀▶ листают); звук состояния ТОЛЬКО BGS — на карточке одна кнопка с именем файла, она открывает меню выбора музыки (список audio/bgs + громкость + радиус прямо в меню, выбор файла применяет всё разом, «Убрать звук» чистит); HP — числом; секции ниже — СВОРАЧИВАЕМЫЕ `<details>` по умолчанию закрыты (Внешний вид/Атаки/Урон/Сенсоры/Паника; `InspectorForm.section(caption, {collapsed})` хостит буферизованные строки, двухколоночный текст S28 у обычных секций сохранён). Рантайм-контракт прежний: персист `{name,kind:'bgs',volume,radius}`; легаси-строки — SE@90. Тесты: карусель/фикс-ширина/ни селектов-ни чисел на карточках/имя на кнопке смерти/пикер с громкостью+радиусом/«Убрать»/5+ свёрнутых секций/hp не слайдер — сьют 239, смоук 32/32.)
- P28-шаги-врагов+двери-транзита завершена 2026-08-30: движок `9dacd21` (**(1) шаги врагов**: враги уже ездят по тем же пулам террейна, что герой (4 звука + дистанционный фейд + `<step_se:VOL>`); НОВОЕ — персональные пресеты: тег `<step_snds:A,B,C,D>` в note события (парсится и из note, и из комментариев страниц) заменяет пул террейна для этого врага и работает даже на террейне без своих звуков (`_stepPoolOverride` в getStepSound, мусорные/пустые списки игнорируются); карточка врага получила свёрнутую секцию «Шаги» (громкость + 4 слота SE имён → `stepSounds`); заглушки редактора (`createEnemyStub`) и ИНЖЕКТНЫЕ враги погони несут теги в note (`SDE_API.getTemplate`; инжектный note почищен от двойных скобок) — материализованный преследователь ходит со звуком. **(2) анимация дверей при транзите**: враг входит в дверь (enterTransit) и материализуется у парной — дверь играет SE (параметры Door SE=Door1 / Door SE Volume=90; в audio/se есть Door1-4, Open1-5) и чарсет анимируется открывание-закрывание — кадровый тикер крутит `setPattern 0→1→2(пауза)→1→0` прямо на спрайте двери, НЕ трогая её сложный список команд (двери тут `!SF_Door`-чарсеты; события без чарсета деградируют до звука). Поиск парной двери: сначала статические рёбра графа, затем рантайм-скан — любой ивент с чарсетом и командой 201 на любой странице в радиусе 1.6 (**живой урок: парные двери часто autorun-триггер 2 — в статический граф не входят**). Живое доказательство: биба на 45-й, герой через ev7 → на 129-й лифт (ev5, триггер 2, найден рантайм-сканом) анимируется паттернами 1,2,1,0 + SE Door1, инжектный враг несёт `<step_se>` с вооружёнными шагами. Тесты: пул-оверрайд (note/комментарий/мусор), doorFx SE+последовательность, инжект с тегами — сьют 244, смоук 32/32. Готча: зомби-процессы NW копятся между пробами — киллить перед прогоном.)
- P29-поверхность-шагов+свой-SE-дверей завершена 2026-08-30: движок `fab1b30` (**(1) шаги врагов учитывают поверхность и скорость**: пресет `<step_snds>` больше НЕ подменяет пул террейна — поверхность прежде всего, ровно как у героя: настроенные террейны играют свои пулы, пресет — фоллбек для поверх без звуков; скорость уже общая с героем — модификаторы бега (≥5 громче/чаще) и шагов (≤3) едут на moveSpeed страницы скелета (speedCalm/speedCombat)). **(2) дверь открывается СВОИМ SE** — тем же, что слышит герой: первая команда 250 внутри ивента двери; приоритет: статическое ребро графа (SE теперь парсится из ФАЙЛОВ вместе с ребром — рантайм-страницы дверей переписываются чужими плагинами) → рантайм-активная страница → параметр Door SE. Живое доказательство: входная дверь 45-й сыграла свой Open3 (из файла — её рантайм-страница переписана), лифт на 129-й — свой Move3 + анимация паттернов; Door1-фоллбек не понадобился. Тесты: пул террейна побеждает пресет (голая поверхность — фоллбек на пресет), парсер выносит SE в ребро, приоритет статическое-ребро→рантайм→параметр — сьют 245, смоук 32/32.)
- P30-AEX-шаги+фикс-заморозки завершена 2026-08-30: движок `070945c` (**(1) шаги врагов затухают через OcRam AEX** (механизм натива 728): для событий с шагами playStepSE строит AEX-объект на якоре-событии — полная громкость в пузыре 1 тайл у источника, плавный фейд до Max Hearing Distance, стерео-автопан, живая позиция — `AudioManager.playSe(se, aex)`; без OcRam — прежний линейный фейд, герой не тронут. **(2) ФИКС ЗАМОРОЗКИ ВРАГА** («враг не двигается»): у карточки бибы в проекте `speedCalm='0'` → скелетные страницы moveSpeed 0 → MV `distancePerFrame = floor(2^0/256 × ALTIMIT_PRECISION=128)/128 = РОВНО 0` → каждый `moveVector` получал вектор (0,0) и враг стоял вечно (поймано живым перехватчиком moveVector: 25 вызовов, все (0,0), ноль смещения; скорость при этом «есть», vel≠0 — потому и выглядело как «упирается»). Скелет теперь клэмпит скорости карточки 1..6 (мусор → дефолты 3/4); карточка бибы в проекте поправлена на speedCalm=3 (точечная правка данных). Живое доказательство: биба moveSpeed=3 идёт к герою (2.6 тайла, прибытие стопает погоню) и шаркает позиционными AEX-шагами (8 спипых playSe(se,aex): eventId-якорь, фейд 15, автопан); на закоммиченном коде до фикса — заморожена. Готча: ALTIMIT_PRECISION=128 — ЛЮБАЯ скорость ≤1/512 обнуляется floor'ом; grep-ловушка: `speed: N` в скелете живёт в страницах как `moveSpeed`. Тесты: AEX-путь (объект с eventId/distance/radius/pan), фоллбек без OcRam, герой без изменений, клэмп 0/99 → 3/4 — сьют 248, смоук 32/32.)
- P31-доигрывание-скина-удара завершена 2026-08-30: движок `1e3d015` (**оживлен «Свитч 3 (Доигрывание)» Спрайтера** — по репорту юзера «анимация удара обрывается на двух кадрах; пусть спрайтер выключит свитч, как только анимация закончится — такой функционал был?»). Механика была и жила: `checkSpecialSwitch` перехватывает `Game_Switches.setValue` для свитча `anim.active.sw3`, ставит `anim.lock` — ручная анимация скина доигрывает до полного круга (`anim.idx===0`), потом `reevaluate()` возвращает базовый облик. НО парсер условий S32 **не заполнял sw3** у скина — `undefined === id` всегда false, скин обрывался в момент выключения свитча (= «два кадра» юзера). Фикс: парсер скинов героя несёт `sw3 = легаси SwitchId3` (именно так параметр назывался в оригинале). Данные проекта (не коммитить): «Лом-Комбез-УДАР»/«Ствол-Комбез-ОГОНЬ» получили `Checks=[sw15]` (замах без прицела) + `SwitchId3=15`; CE#12 ветка лома: `sw15 ON → стамина → 726 РЫВОК УдарЛома сразу (его Wind7) → 730 wait 4 → performMelee + SE → sw15 OFF` — замах доигрывает Спрайтер сам. Бонус: AgoniaDebugKit — автостарт с ретраем в `Scene_Boot.update` (start() одномоментен и стрелял до асинхронной загрузки базы — гонка оставляла дебаг-прогоны на титуле). Тест: sw3 парсится, скин-замах матчится при sw15, база возвращается после. Сьют 249, смоук 32/32. Юзер просил живые прогоны НЕ запускать самому — просить его.)
- История откатов через `git reset --hard` — перед крупными правками фиксировать тег/коммит.
- Память проекта: глобальный vault `~/loam-memory` (qmd `global-memory`), страницы `agonia-*`.

## GitHub

- Remotes: `origin` = NotAbeli/RPGReactor (main — канон Agonia Engine), `upstream` = Psychronic-Games/RPGReactor (жив, 0.98.1 при нашей базе 0.96.0 — cherry-pick выборочно).
- Аутентификация HTTPS: Git Credential Manager. `gh` CLI не залогинен; `$env:GH_TOKEN` через `"protocol=https`nhost=github.com`n`n" | git credential fill`.
- Сеть сбоит перемежающе (DPI) — ретраи; запасной транспорт SSH-over-443 в `~/.ssh/config`.
- Коммитить только по явной просьбе; пушить в origin после завершённых блоков работы.
